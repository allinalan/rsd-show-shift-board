Read CLAUDE.md first. The project rules and safety gates in CLAUDE.md apply to Codex sessions too.
Production runs on the Mac mini only (~/automations/rsd-show-shift-board, daily tick at 07:00); any other checkout is a development clone. GitHub is the source of truth, and the mini pulls only over a clean tree, so leave `git status` empty.
This repo is PUBLIC: no phone numbers, e-mails, contact fields, reports, or anything from seed/private/. The pre-commit leak check enforces it; never bypass it.
Routines never send messages. Never commit ~/.rsd/board.env or a service key.
