backups/
.DS_Store
*.log
node_modules/
# machine-local, never public
seed/private/
logs/
out/
PAUSED
# another agent tool's local copies of CLAUDE.md and the skills. Untracked files make the tree
# dirty, and deploy/tick.py never pulls over a dirty tree, so they must not count as changes.
.agents/
AGENTS.md
